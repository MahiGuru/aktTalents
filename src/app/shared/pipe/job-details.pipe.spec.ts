import { JobDetailsPipe } from './job-details.pipe';

describe('JobDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new JobDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
